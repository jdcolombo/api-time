# api-time
A JS function to hide information depending on the current time anda day of the week. It uses a time api (https://worldtimeapi.org) but if it is not available or shows an error, it uses the user's computer time.
